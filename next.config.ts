/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "api.qrserver.com",
            "upload.wikimedia.org",
            "cdn-icons-png.flaticon.com"
        ],
    },
};

module.exports = nextConfig;
